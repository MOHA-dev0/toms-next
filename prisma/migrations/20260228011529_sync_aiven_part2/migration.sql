/*
  Warnings:

  - The primary key for the `_QuotationDestinations` table will be changed. If it partially fails, the table could be left without primary key constraint.

*/
-- AlterTable
ALTER TABLE `_QuotationDestinations` DROP PRIMARY KEY;
